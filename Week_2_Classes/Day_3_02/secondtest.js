// target the ul
// inside the button onClick:
// create li - we want to create the li's when we click the button
// insert/append into the list

/* let myUl = document.getElementById('item-list');
myUl.appendChild(list1)
console.log(myUl);
let addButton = document.getElementById('add-item');

addButton.onclick = function () {
    let value = 0;
    for (let i = 0; i < value; i++;) {
        value++;
    }
    let list1 = document.createElement('li');
    list1.innerHTML = 'item number ${value}';
} */

//

/* let ulTag = document.getElementById('item-list');
let count = 1;
addButton.onclick = function () {
    let newLi = document.createElement('li')
    newLi.innerHTML = 'item number ${count}';
    count++;
    ulTag.appendChild(newLi)
} */